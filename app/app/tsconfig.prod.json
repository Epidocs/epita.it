{
  "extends": "./tsconfig.json",
  "exclude": [
    "${configDir}/dist",
    "${configDir}/test",
    "${configDir}/playwright*.ts",
    "${configDir}/vitest*.ts",
    "${configDir}/**/*.test.ts",
    "${configDir}/**/*.spec.ts"
  ]
}
